# Product browser tests

Add real product journeys here. This directory is intentionally editable and
may contain no tests before product work starts. The protected platform
regression lives in tests/foundation and always runs in CI.
